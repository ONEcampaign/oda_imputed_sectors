[tool.poetry]
name = "oda-imputed-sectors"
package-mode = false
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.2.2"
oda-data = "^1.3.3"
pyarrow = ">=16.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
